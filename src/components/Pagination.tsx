function Pagination() {
    // const { current, onChange, total } = props;
  
    return (
      <div
        // current={current}
        // onChange={onChange}
        // total={total}
      ></div>
    );
  }

export default Pagination;